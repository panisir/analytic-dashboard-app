# Step 1: Log in to Docker Hub
docker login

# Step 2: Build the Docker image (replace with your details)
docker build -t panisir/dashboard-app:latest .

# Step 3: Push the Docker image to Docker Hub
docker push panisir/dashboard-app:latest